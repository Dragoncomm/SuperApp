name: Print Secret Variable - UserX

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print Secret
        run: |
          echo "The GitHub Action Secret will be masked:  "
          echo ${{ secrets.PASSWORD }}
          echo "Trick to echo GitHub Actions Secret:  "
          echo ${{ secrets.PASSWORD }} | sed 's/./& /g'
